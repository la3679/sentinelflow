{
  "name": "sentinelflow",
  "version": "0.1.0",
  "private": true,
  "description": "SentinelFlow - an educational, event-driven transaction-risk and fraud-operations platform built on synthetic data.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "engines": {
    "node": ">=24",
    "bun": ">=1.4.0"
  },
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "web": "bun run --filter sentinelflow-web"
  },
  "devDependencies": {
    "@apidevtools/swagger-parser": "^12.1.0",
    "@asyncapi/parser": "^3.6.3",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "prettier": "^3.7.3"
  }
}
